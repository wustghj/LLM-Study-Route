# LLM 学习路径 — 纯小白自顶向下完整版

> 目标读者：对 AI 大模型感兴趣，但零基础的普通人
> 核心原则：先动手跑起来，再理解原理。先会用，再会改，再会造。

---

## 阅读指南

这份学习路径分 **7 层**（0→6），每层都建立在前一层之上。**不要跳层。**

- 如果你是纯小白：从 Pre-Phase 开始，5 分钟跑通第一个对话
- 如果你已经用过 ChatGPT/DeepSeek：从 Phase 0 开始补基础认知
- 如果你已经会调 API：跳到 Phase 2 学推理引擎

每个阶段末尾都有 **验收题**——能答上来再进入下一阶段。

---

## 全景图

```
                        ┌──────────────────────────────────────────┐
                        │     6. 进阶深造（Transformer/训练/多模态）   │
                        ├──────────────────────────────────────────┤
                        │     5. 生产化（性能/成本/监控/安全）         │
                        ├──────────────────────────────────────────┤
                        │     4. 模型定制（Prompt工程/微调）           │
                        ├──────────────────────────────────────────┤
                        │     3. 应用框架（RAG/Agent/工具调用）        │
                        ├──────────────────────────────────────────┤
                        │     2. 推理引擎（模型怎么"跑"起来的）         │
                        ├──────────────────────────────────────────┤
                        │     1. 上手使用（API/本地模型/代理）          │
                        ├──────────────────────────────────────────┤
                        │     0. 基础认知（它是什么/能做什么/不能做什么） │
                        ├──────────────────────────────────────────┤
                        │     😃 Pre-Phase：5分钟快速体验             │
                        └──────────────────────────────────────────┘
```

---

## Pre-Phase：5 分钟快速体验（先跑起来）

> 目标：用 DeepSeek 完成一次对话。不解释原理，先建立"它是什么"的体感。

### 第 1 步：获取 API Key（1 分钟）

打开 [DeepSeek 开放平台](https://platform.deepseek.com)，注册账号，获取一个 API Key（形如 `sk-xxxxxxxx`）。

> API Key 就是你的"门禁卡"——没有它，服务器不让你用模型。

### 第 2 步：设置环境变量（1 分钟）

打开 PowerShell，输入（把 `sk-你的key` 换成真实的 key）：

```powershell
$env:DEEPSEEK_API_KEY = "sk-你的key"
```

### 第 3 步：跑一次对话（3 分钟）

```powershell
cd api调用实战/cli-chat
pip install openai tomli
python main.py --config config.example.toml
```

输入一个问题，比如 `"用一句话解释什么是人工智能"`，然后看：
- 屏幕上逐字出现回答（这就是**流式输出**）
- 回答结束后看到 `[metrics] first_token_ms=xxx, total_ms=xxx`

> 恭喜！你已经完成了一次 LLM API 调用。

### 你刚才做了什么？

```
你输入文字 → 电脑打包成 HTTP 请求 → 发送给 DeepSeek 服务器
→ DeepSeek 的模型读你的问题 → 一个字一个字生成回答 → 通过网络传回你的屏幕
```

就这么简单。后面所有阶段都在**拆解这个过程的每一步**。

---

## Phase 0：基础认知（1-3 天）

> 目标：知道 LLM 是什么、能干什么、不能干什么。建立基本的术语体系。

### 0.1 LLM 到底是什么？

**一句话版本：** LLM（Large Language Model，大语言模型）是一个"根据上文猜下文"的系统。

你给它一段话的开头，它预测最合理的后续内容。就像输入法里的联想词功能——但强大几万倍。

**一个比喻：**

把 LLM 想象成一个读了整个互联网的人。你问他一个问题，他不是去数据库里搜索答案，而是根据他读过的所有内容，**预测**什么样的回答最合理。

> 这就是为什么它会"幻觉"（说自己不知道的事却编得像真的）——它不是搜索引擎，它是预测引擎。

### 0.2 端到端：一次对话到底发生了什么

```
你输入："用一句话解释什么是注意力机制"
  │
  ▼
┌──────────────────────────────────────────────────────┐
│ 1. Tokenizer（分词器）                                  │
│    把文字切成小块（token）                                │
│    "用一句话解释什么是注意力机制" → [用, 一句, 话, 解释, ...]  │
│    约 12 个 token                                       │
├──────────────────────────────────────────────────────┤
│ 2. 模型推理（Model Inference）                           │
│    把所有 token 送进神经网络，一层层计算                     │
│    输出：下一个最可能的 token 是什么？                      │
├──────────────────────────────────────────────────────┤
│ 3. 采样（Sampling）                                     │
│    模型给出的是概率分布，不是确定的答案                      │
│    Temperature 控制"创造性"——越低越保守，越高越随机         │
├──────────────────────────────────────────────────────┤
│ 4. 循环生成                                             │
│    拿到一个新 token，拼到末尾，再预测下一个                  │
│    直到产生"结束"信号或达到长度上限                         │
└──────────────────────────────────────────────────────┘
  │
  ▼
输出："注意力机制是一种让模型在处理信息时能够自动..."
```

### 0.3 必知必会的 6 个术语

| 术语 | 一句话解释 | 你什么时候会用到 |
|------|-----------|----------------|
| **Token** | 模型的最小处理单元，≈0.75 个中文字 | 计费、限制长度 |
| **上下文窗口** | 模型一次能"看到"的最大 token 数 | 长文档处理时会碰到限制 |
| **Temperature** | 控制输出随机性（0=死板，1=放飞） | 写代码用低值(0.1)，写文案用高值(0.8) |
| **流式输出** | 边生成边返回，不等全部生成完 | 所有聊天产品都在用 |
| **幻觉** | 模型自信地说出错误信息 | 不要盲目信任模型的回答 |
| **Prompt** | 你发给模型的指令/问题 | 你每天都在写 |

### 0.4 如何判断一个模型"好不好"？

三个维度，从用户角度：

| 维度 | 什么意思 | 怎么看 |
|------|---------|--------|
| **快不快** | 首 token 延迟 + 生成速度 | 体感等待时间 |
| **准不准** | 回答是否准确、有用 | 用你熟悉的话题测试 |
| **贵不贵** | 每 1000 token 的价格 | 查 API 定价页 |

> 本项目的 CLI 客户端每次回答后都输出 `first_token_ms` 和 `total_ms`，帮你建立"延迟"的体感。

### Phase 0 验收

- [ ] 能用自己的话解释"LLM 是什么"，不超过 3 句话
- [ ] 能画出"用户输入 → 模型输出"的完整链路
- [ ] 知道 token、temperature、上下文窗口、幻觉 这四个词的意思
- [ ] 亲自跑通了 Pre-Phase 的快速体验

---

## Phase 1：上手使用（1-2 周）

> 目标：熟练调用 LLM API，理解 HTTP 请求/响应的结构，能对比不同 provider 的性能。

```
┌────────────────┐     ┌────────────────┐     ┌────────────────┐
│  DeepSeek 云    │     │  Ollama 本地    │     │  Proxy 代理     │
│  (最快最省心)    │     │  (免费但需要GPU) │     │  (中间层转发)    │
└───────┬────────┘     └───────┬────────┘     └───────┬────────┘
        │                      │                      │
        └──────────────────────┼──────────────────────┘
                               │
                    同一个客户端，只改配置
```

### Track 1A：用 SDK 调用 API（1 天） ✅ 已完成

**做什么：** 跑通 `cli-chat/main.py`

**你学到的：**
- 什么是 API Key 鉴权
- 如何配置 base_url、model、temperature
- 流式输出的体验差异
- 多轮对话如何保持上下文（把历史消息一起发回去）

**动手试试：**
1. 修改 `config.toml` 里的 `temperature`，分别设为 0.1 和 0.9，问同一个问题，看看输出有什么不同
2. 连续对话 5 轮，观察 `total_ms` 的变化趋势
3. 给 `system_prompt` 设置不同角色（"你是一个毒舌的代码审查员" vs "你是一个耐心的小学老师"）

### Track 1B：不用 SDK，手写 HTTP（1 天） ✅ 已完成

**做什么：** 跑通 `cli-chat/raw_client.py`

**为什么要做：** SDK 帮你封装了很多细节。要真正理解协议，你需要自己写一遍。

**你学到的：**
- HTTP POST 请求的完整结构（Header、Body、URL）
- SSE（Server-Sent Events）协议——`data:` 前缀、`[DONE]` 终止符
- 手动解析流式数据

> 🧠 **核心理解：** SDK 帮你做的只有三件事：构造 HTTP 请求、解析 SSE 流、处理错误。其他都是你自己的代码。

### Track 1C：本地模型部署（1 天） ✅ 已完成

**做什么：** 安装 Ollama，拉取并运行本地模型

**为什么要做：** 云 API 是别人的，本地模型是你自己的。理解"本地推理"和"云推理"的区别。

**环境要求：** 至少 16GB 内存，推荐有 NVIDIA 显卡。

```powershell
# 1. 安装 Ollama（去官网下载 Windows 安装包）
# 2. 拉取模型
ollama pull qwen2.5:7b

# 3. 测试
ollama run qwen2.5:7b
```

**你学到的：**
- 为什么要量化（原始模型太大，消费级硬件跑不动）
- 显存/内存对推理的影响（不够大就跑不了大模型）
- 本地推理延迟 vs 云 API 延迟的差异

### Track 1D：多 Provider 性能对比（0.5 天） ✅ 已完成

**做什么：** 运行 `cli-chat/benchmark.py`，拿到 CSV 数据

**你学到的：**
- 同一个 prompt，DeepSeek 可能 300ms 出第一个字，本地 Ollama 要 5 秒——为什么？
- token/s 衡量的是什么
- 用数据而非感觉说话

### Phase 1 验收

- [ ] 能用同一个客户端，只改 config.toml，成功调用 DeepSeek、Ollama、Proxy 三种后端
- [ ] 能解释 HTTP 请求里 `messages`、`stream`、`temperature` 的含义
- [ ] 能写出 SSE 数据流的格式（`data: xxx` / `data: [DONE]`）
- [ ] benchmark.csv 里有至少 3 个 provider 的数据，能说出谁的延迟最低

---

## Phase 2：推理引擎（2-4 周）

> 目标：打开"模型是怎么跑起来的"这个黑盒，理解推理的两个阶段。

```
你的问题 ──→ [Prefill] 并行处理整个输入 ──→ [Decode] 逐 token 生成

  Prefill（预填充）：计算密集型                      Decode（解码）：内存密集型
  ┌──────────────────────────┐               ┌──────────────────────────┐
  │ 一次性算完所有输入 token    │      ──→      │ 每次只算 1 个新 token      │
  │ 类似"一口气读完整个问题"    │               │ 类似"边想边说"            │
  └──────────────────────────┘               └──────────────────────────┘
```

### Track 2A：认识 llama.cpp ✅ 已完成

**一句话：** llama.cpp 是 Ollama 的底层引擎，纯 C/C++ 写的推理框架。

**你学到的（`llama-cpp-notes/getting-started.md`）：**
- GGUF 文件是什么（量化打包后的模型文件，一个文件包含所有权重）
- 量化等级怎么看（Q2_K < Q4_K_M < Q8_0，数字越大质量越好、文件越大）
- `-c`（上下文大小）、`-ngl`（GPU 层数）、`-t`（线程数）的作用

### Track 2B：KV Cache —— 推理的核心 ✅ 已完成

**核心问题：** 为什么对话越长越慢？为什么上下文不能无限长？

**直觉理解（`llama-cpp-notes/kv-cache-deep-dive.md`）：**

想象你在写一篇文章。每写一句，你不需要重新读一遍全文——你记得前面写了什么。

LLM 也是这样：它把之前计算过的中间结果（Key 和 Value 向量）缓存起来，生成新 token 时只需"看一眼"缓存，不用重新算。这个缓存就是 **KV Cache**。

**关键数字：**
- 上下文 4096 → KV Cache ≈ 1.6GB（以 Qwen2.5-7B 为例）
- 上下文 8192 → KV Cache ≈ 3.2GB（翻倍）
- 显存不够 → 要么截断、要么报错

> 🔧 **动手验证：** 启动 llama.cpp server 时改 `-c` 参数，去任务管理器看显存变化。

### Track 2C：动手做实验 ✅ 已完成

**做什么：** 按 `llama-cpp-notes/experiments.md` 的 6 个实验跑一遍

**核心实验：**
| 实验 | 变量 | 你会看到什么 |
|------|------|------------|
| GPU 层数 | `-ngl 0 vs 35` | CPU 推理慢到怀疑人生，GPU 快几十倍 |
| 上下文长度 | `-c 2048 vs 8192` | 上下文翻倍，显存几乎翻倍 |
| 量化等级 | Q2 vs Q4 vs Q8 | 文件大了质量好了，速度变化不大 |
| Ollama vs 原生 | 对比延迟 | Ollama 多一层 IPC，百毫秒级损失 |

### Phase 2 验收

- [ ] 能描述推理的两个阶段（Prefill 做了什么，Decode 做了什么）
- [ ] 能解释 KV Cache 为什么让长对话变慢
- [ ] 能用数据回答：你的机器上，GPU 加速（-ngl）对首 token 延迟帮助大还是对吞吐帮助大？
- [ ] 至少跑过 3 组对照实验并填好了结果表

---

## Phase 3：应用框架（2-4 周）

> 目标：从"调 API"升级到"构建 LLM 应用"。

### Track 3A：RAG —— 让模型知道你的数据 ✅ 已完成

**核心问题：** 模型训练数据有截止日期，也不知道你公司的内部文档。怎么办？

**RAG（Retrieval-Augmented Generation，检索增强生成）** 的思路：

```
用户问："公司今年的报销政策是什么？"
  │
  ├─→ 1. 把问题向量化（转成一串数字）
  │
  ├─→ 2. 在公司文档库里搜索最相似的内容
  │
  ├─→ 3. 把搜到的内容 + 问题 一起发给 LLM
  │
  └─→ 4. LLM 基于"刚给的资料"回答
```

> 🧠 **核心理解：** RAG 不是让模型变聪明，而是**让模型看到它本来不知道的信息**。本质上是在 prompt 里偷偷塞了一份参考答案。

**项目：** `rag-demo/rag.py`

你会在代码里看到：
- 文档分块（chunking）——为什么不能把整本书塞进去？
- 向量化（embedding）——怎么把文字变成可比较的数字？
- 相似度检索——怎么找到"最相关的"那段话？
- 对比模式（`--compare`）——不加 RAG vs 加 RAG，效果天差地别

### Track 3B：Agent —— 让模型能做事 ✅ 已完成

**核心问题：** 模型只能输出文字，不能执行操作。Agent 解决了这个问题。

**Agent 的核心循环（Think → Act → Observe）：**

```
用户："帮我算 12345 × 6789"
  │
  ▼
┌─────────────────────────────────────────────┐
│ Think（思考）                                   │
│   LLM："这是个数学题，我应该用计算器工具"          │
├─────────────────────────────────────────────┤
│ Act（行动）                                     │
│   程序调用 calculator("12345 * 6789")           │
│   结果：83810205                               │
├─────────────────────────────────────────────┤
│ Observe（观察）                                  │
│   LLM 看到结果："答案已知，可以回复用户了"          │
├─────────────────────────────────────────────┤
│ Answer（回答）                                   │
│   "12345 × 6789 = 83810205"                   │
└─────────────────────────────────────────────┘
```

> 🧠 **核心理解：** LLM 只负责**决策**（"我该调用什么工具？用什么参数？"），不负责**执行**。执行是你写的代码。

**项目：** `agent-demo/agent.py`

你会看到：
- 工具定义（JSON Schema）——告诉模型"你可以用这些工具"
- tool_choice——模型可以选择答话或调工具
- 多轮循环——一次工具调用不够，继续调直到得到答案

### Phase 3 验收

- [ ] 能画出 RAG 的流程图（分块→向量化→检索→拼接→LLM 回答）
- [ ] 能解释 Agent 的 Think→Act→Observe 循环
- [ ] 运行 `rag.py --compare` 看到了 RAG vs 无 RAG 的差异
- [ ] 运行 `agent.py`，亲眼看到它调用 calculator 算出了正确结果

---

## Phase 4：模型定制（2-4 周）

> 目标：让模型按你的要求做事，而不是你适应模型。

### Track 4A：Prompt Engineering

**核心：** 怎么写 prompt 让模型输出你想要的结果？

5 种基本技巧：

| 技巧 | 做法 | 示例 |
|------|------|------|
| **角色设定** | 告诉模型它是谁 | "你是一个资深后端工程师..." |
| **Few-shot** | 给 2-3 个输入输出示例 | "问题：xxx → 答案：xxx" |
| **Chain-of-Thought** | 要求分步骤 | "请一步一步思考" |
| **格式约束** | 指定输出格式 | "只输出 JSON，不要其他文字" |
| **迭代优化** | 写→看→调→再看 | 没有一次写对的 prompt |

### Track 4B：LoRA 微调 ✅ 已完成

**LoRA（Low-Rank Adaptation）= 低成本微调**

**直觉理解：** 原始模型有十亿个参数，全部重训太贵。LoRA 的做法是——冻结所有原始参数，只训练一小撮新增的（约 1%），就能让模型学会新技能。

就像给一本已经写好的书加一些便利贴——书本身不改，但贴上便利贴后阅读体验变了。

**项目：** `finetune-demo/`（三阶段流水线）

1. `prepare_data.py` —— 生成 200 条训练数据，教你用"简洁 C++ 工程师口吻"回复
2. `train.py` —— QLoRA（4bit 量化 + LoRA），~4GB 显存就能跑
3. `inference.py` —— 微调前后对比，直观看到差异

**你会学到：**
- 训练数据长什么样（JSONL 对话格式）
- LoRA rank、alpha、target_modules 是什么意思
- 为什么 QLoRA 能让低显存也能微调（先量化到 4bit 再训 LoRA）
- 微调前后模型行为差异有多大

### Phase 4 验收

- [ ] 能设计一套 system prompt，让模型稳定输出你想要的风格/格式
- [ ] 能解释 prompt engineering 和微调的区别（一个不改变模型，一个改变）
- [ ] 成功生成训练数据并完成一次 LoRA 微调
- [ ] 看到微调前后模型在目标行为上有明显差异

---

## Phase 5：生产化（2-3 周） 📋 待规划

> 目标：能设计、部署、运维一个真实的 LLM 服务。

### 你要回答的核心问题

1. **成本：** 一次对话花了多少钱？（输入 token × 输入单价 + 输出 token × 输出单价）
2. **延迟：** 用户等了多久？首 token 延迟和总延迟哪个更重要？
3. **并发：** 10 个用户同时用，系统撑得住吗？
4. **安全：** 用户输入能"控制"模型行为吗？（Prompt Injection）
5. **监控：** 怎么知道模型回答质量在变差？

### 计划内容

- 结构化日志（JSON log，可工具解析）
- 成本追踪（每次请求统计 token 消耗和花费）
- 基本安全（输入过滤、Rate Limiting、API Key 管理）
- 模型版本管理（怎么切换、怎么回滚）

---

## Phase 6：进阶深造（长期） 🔭 远期

> 目标：深入原理，具备独立研究和优化能力。

### 推荐顺序（不要从论文开始！）

1. **先看图解：** [The Illustrated Transformer](http://jalammar.github.io/illustrated-transformer/) ← 有中文翻译
2. **再看视频：** [3Blue1Brown 注意力机制系列](https://www.youtube.com/playlist?list=PLZHQObOWTQDNU6R1_67000Dx_ZCJB-3pi)
3. **最后读论文：** "Attention Is All You Need"

### 学习专题

| 专题 | 核心问题 |
|------|---------|
| Transformer 架构 | Attention 到底是怎么算的？为什么能并行？ |
| 训练三阶段 | 预训练→SFT→RLHF 各做什么？为什么需要三个阶段？ |
| 多模态 | 图片/语音怎么"喂"给 LLM？ |
| 模型压缩 | 量化/蒸馏/剪枝——怎么让大模型跑在手机上？ |

---

## 学习时间线建议

| 时间 | 阶段 | 产出 |
|------|------|------|
| 第 1 天 | Pre-Phase + Phase 0 | 跑通一次对话，理解基本术语 |
| 第 1 周 | Phase 1 | 熟练调 API，手写 HTTP，部署本地模型 |
| 第 2-3 周 | Phase 2 | 理解推理引擎，跑完 6 个实验 |
| 第 4-5 周 | Phase 3 | 实现 RAG 和 Agent |
| 第 6-8 周 | Phase 4 | Prompt 工程 + LoRA 微调 |
| 第 9-12 周 | Phase 5 | 生产化实践 |
| 长期 | Phase 6 | 按兴趣深入 |

---

## 每个阶段的核心提问

完成一个阶段后，你应该能回答：

| 阶段 | 核心问题 |
|------|---------|
| Phase 0 | LLM 和搜索引擎的本质区别是什么？ |
| Phase 1 | API 请求报文长什么样？流式响应和普通响应区别在哪？ |
| Phase 2 | 为什么对话越长越慢？量化到底量化了什么？ |
| Phase 3 | RAG 和 Agent 分别解决什么问题？ |
| Phase 4 | 微调和调 prompt 的根本区别是什么？ |
| Phase 5 | 推理成本怎么算？线上服务的瓶颈在哪？ |
| Phase 6 | Attention 机制的本质是什么？ |

---

## 附录 A：术语速查表

| 术语 | 一句话解释 |
|------|-----------|
| **API** | 应用程序接口——你发请求，服务器返回结果 |
| **API Key** | 你的身份凭证——证明"是你，你可以用" |
| **Embedding** | 把文字变成一串数字，语义相近的文字数字也相近 |
| **GGUF** | llama.cpp 用的模型文件格式，量化后的模型打包 |
| **JSON/JSONL** | 数据交换格式，JSONL 是每行一个 JSON |
| **Ollama** | 让你在个人电脑上轻松运行 LLM 的工具 |
| **OpenAI-Compatible API** | 一套"行业标准"的接口规范，大家都按这个来 |
| **Prefill** | 推理第一阶段：一次性处理全部输入 |
| **Decode** | 推理第二阶段：逐 token 生成输出 |
| **SSE** | 服务器推送事件——允许服务器持续发数据给客户端 |
| **SDK** | 软件开发工具包——别人写好的代码，帮你少写重复代码 |
| **System Prompt** | 给模型设定的"人格"——在所有用户消息之前发送 |
| **TOML** | 一种配置文件格式，比 JSON 更适合人类读写 |
| **vLLM** | 一个高性能 LLM 推理框架，核心优化是 Continuous Batching |

## 附录 B：常见问题排查

| 问题 | 可能原因 | 解决办法 |
|------|---------|---------|
| `认证失败` | API Key 没设置或已过期 | 检查 `$env:DEEPSEEK_API_KEY` 是否设置 |
| `网络连接失败` | 没开代理/VPN，或 base_url 写错 | 检查 config.toml 的 base_url |
| `请求超时` | 网络不稳定或模型响应太慢 | 重试，或换更快的模型 |
| Ollama 下载模型很慢 | 国内网络问题 | 用镜像站，或从其他渠道下载 GGUF 文件手动导入 |
| `CUDA out of memory` | 显存不够 | 减小上下文长度（-c），或用量化更低的模型 |
| `ModuleNotFoundError` | 没装依赖 | 检查对应目录的 requirements.txt，run `pip install -r` |

## 附录 C：项目目录总览

```
api调用实战/
│
├── cli-chat/                         # Phase 1：API 调用客户端
│   ├── main.py                       #   SDK 版多轮对话
│   ├── raw_client.py                 #   纯 HTTP 版（手写协议）
│   ├── benchmark.py                  #   多 provider 性能对比
│   ├── config.toml / config.example.toml
│   └── conversations/                #   对话存档
│
├── proxy-test/                       # Phase 1：代理验证
│   ├── test_chat.py / test_stream.py
│
├── ollama-notes/                     # Phase 1：本地部署笔记
│   ├── windows-setup.md
│   └── model-benchmark.md
│
├── llama-cpp-notes/                  # Phase 2：推理引擎笔记
│   ├── getting-started.md            #   编译与运行
│   ├── kv-cache-deep-dive.md         #   KV Cache 深度解析
│   └── experiments.md                #   对照实验设计
│
├── rag-demo/                         # Phase 3：RAG 系统
│   └── rag.py
│
├── agent-demo/                       # Phase 3：Agent 系统
│   └── agent.py
│
├── finetune-demo/                    # Phase 4：微调流水线
│   ├── prepare_data.py / train.py / inference.py
│
├── LLM学习路径_完整版.md              # ← 你正在看的文件
└── README.md                         #   项目入口
```
