# LLM 从零到懂 — 完整指南

> 30 分钟，零基础。读完你就知道大模型到底是什么、怎么用、怎么造。

---

## 目录

1. [先讲一个故事](#1-先讲一个故事)
2. [LLM 到底是什么](#2-llm-到底是什么)
3. [它怎么"看懂"你的话——Tokenizer](#3-它怎么看懂你的话tokenizer)
4. [它怎么"理解"意思——Embedding](#4-它怎么理解意思embedding)
5. [它怎么"想"出答案——Transformer](#5-它怎么想出答案transformer)
6. [它怎么被教出来的——训练的故事](#6-它怎么被教出来的训练的故事)
7. [它有哪些做不到的事](#7-它有哪些做不到的事)
8. [怎么用它——三种方式](#8-怎么用它三种方式)
9. [怎么让它更厉害——RAG、Agent、微调](#9-怎么让它更厉害ragagent微调)
10. [下一步](#10-下一步)

---

## 1. 先讲一个故事

2017 年，Google 的几个研究员发表了一篇论文，标题叫《Attention Is All You Need》。他们提出了一种新的神经网络结构，叫 **Transformer**。

当时没人想到，这篇论文会改变世界。

2022 年底，一家叫 OpenAI 的公司，用 Transformer 结构训练了一个叫 GPT-3.5 的模型，给它做了个聊天界面叫 ChatGPT。两个月内，1 亿人开始使用它——这是人类历史上增长最快的产品。

到今天，大模型（LLM，Large Language Model）已经能：
- 写代码、改 bug、解释代码
- 翻译几十种语言
- 写文章、写诗、写文案
- 回答各种领域的问题（虽然不总是对的）
- 调用工具（搜索网页、操作电脑、发邮件）

而你，完全可以理解它是怎么做到的。不需要数学博士，不需要会机器学习。

**记住一句话：大模型就是一个"根据上文猜下文"的系统。** 只是它读过了整个互联网，所以"猜"得特别准。

---

## 2. LLM 到底是什么

### 最简定义

```
LLM = 一个函数
输入：一段文字
输出：下一个最可能的字
```

你给它 "今天天气真"，它输出 "好"。
你给它 "1 + 1 ="，它输出 "2"。
你给它 "中国的首都是"，它输出 "北京"。

### 为什么要叫"大"模型

"大"指的是**参数多**。参数就是模型内部用来计算的可调数字——类似 y = ax + b 里的 a 和 b。

| 模型 | 参数量 | 类比 |
|------|--------|------|
| GPT-2 (2019) | 15 亿 | 一本字典 |
| GPT-3 (2020) | 1750 亿 | 一套百科全书 |
| GPT-4 (2023) | ~1.8 万亿（估算） | 一个图书馆 |
| Qwen2.5-7B | 70 亿 | 能塞进你的电脑 |

> B = Billion = 十亿。7B = 70 亿参数。

参数越多，模型越"聪明"——但也越慢、越贵、越需要好硬件。

### 它的核心能力只有一个

**预测下一个 token。**

" Token "是什么？请读下一章。

---

## 3. 它怎么看懂你的话——Tokenizer

### Token 是什么

模型不能直接处理文字。它只能处理数字。所以你需要一个翻译器——**Tokenizer（分词器）**。

```
你输入："我爱学习"

Tokenizer 翻译：["我", "爱", "学习"] → [10532, 38102, 8573]
```

这三个数字就是三个 token。模型拿到的不是汉字，是数字。

### 中文和英文的巨大差异

```
英文："I love learning" → ["I", " love", " learning"] → 3 个 token
中文："我爱学习"       → ["我", "爱", "学习"]        → 3 个 token

但是：
英文："Hello world"    → ["Hello", " world"]        → 2 个 token
中文："你好世界"       → ["你", "好", "世", "界"]     → 4 个 token
```

> **关键事实：** 同样的意思，中文通常比英文多 1.5-2 倍的 token。这意味着：用中文调 API **更贵**（输入 token 更多）。

### 为什么 token 很重要

| 你关心的事 | 跟 token 的关系 |
|-----------|----------------|
| **计费** | API 按 token 收费，不是按"字"收费 |
| **上下文长度** | 模型能处理的 token 数有上限（如 128K token） |
| **速度** | token 越多，生成越慢 |
| **截断** | 超出上下文窗口，前面的内容会被丢掉 |

> 想知道一句话有多少个 token？去 OpenAI Tokenizer 页面（platform.openai.com/tokenizer）贴进去看。

---

## 4. 它怎么"理解"意思——Embedding

### 从 Token ID 到向量

拿到 token ID（一个整数）后，模型去查一张巨大的表，把它变成一个**高维向量**（一串数字）。

```
Token ID "10532" → 查表 → [0.23, -0.47, 0.81, 0.15, ..., -0.33]
                                          ↑
                                    几百到几千个数字
```

这个过程叫 **Embedding（嵌入）**。

### 向量的魔力

向量的核心特性：**意思越接近的词，向量也越接近。**

```
"猫"的向量：  [0.8, 0.3, -0.2, ...]
"狗"的向量：  [0.7, 0.4, -0.1, ...]  ← 和"猫"很接近
"汽车"的向量：[0.1, -0.5, 0.9, ...]  ← 和"猫"很远
```

你可以对这些向量做"算术"：

```
"国王" - "男" + "女" ≈ "女王"
"巴黎" - "法国" + "意大利" ≈ "罗马"
```

这是 LLM "理解"语义的基础——不是真的理解，而是把意思编码成了空间中的位置。

### 用一个你能看到的例子

想象你把所有颜色的名字压到 2 个维度（实际上 embedding 有几百到几千维）：

```
                    "愤怒"  (0.9, 0.2)
                      │
    "不满" (-0.2, 0.8)│
                      │
    ──────────────────┼──────────────────
                      │
          "平静" (-0.6, -0.7)
                      │
                 "快乐" (0.4, -0.9)
```

意思相近的词在空间中**靠得更近**。模型不是"读懂"了你的话，而是在高维空间里找到了你的意思对应的那个位置。

---

## 5. 它怎么"想"出答案——Transformer

这是大模型的核心引擎。我把数学拿掉，只讲直觉。

### 整体流程

```
"我爱学习"
  │
  ├─→ Tokenizer → [10532, 38102, 8573]
  │
  ├─→ Embedding → 每个 token 变成一个向量
  │
  ├─→ 位置编码 → 告诉模型"这是第 1 个、第 2 个、第 3 个 token"
  │
  ├─→ N 个 Transformer 层 → 一层层地"思考"
  │     │
  │     ├─ 注意力（Attention）：每个 token 看看其他 token，"谁跟我最相关？"
  │     └─ 前馈网络（FFN）：每个 token 独立思考，"这个位置应该输出什么？"
  │
  ├─→ 输出层 → 给词汇表里每个词打分
  │
  └─→ 选分数最高的 → "学"后面的 token 是 "习"
```

### 注意力（Attention）——一切的核心

这是 Transformer 唯一真正重要的创新。用例子说明：

```
输入："我 把 猫 抱 起来 ， 它 很 ___"

当模型要填这个空时，Attention 做的事情：
  - "我"跟"___"的关系：弱（0.05）
  - "猫"跟"___"的关系：强（0.35）← "猫"是它
  - "它"跟"___"的关系：强（0.60）← "它"直接指代空
  - "抱"跟"___"的关系：中（0.15）

然后模型根据这些权重，综合所有信息，预测下一个字是"乖"或"轻"。
```

**注意力 = 每个 token 给其他所有 token 打分："你跟我的关系有多大？"**

一句话总结 Attention：
> Q（查询）和 K（键）算相似度 → softmax 变成概率 → 用这些概率去加权取 V（值）。

### 为什么它能并行

传统的 RNN（循环神经网络）必须一个字一个字地处理——读完"我"才能读"爱"，读完"爱"才能读"学"。

Transformer 同时看所有 token。这就是它能训练得这么快、这么大的原因——所有 token 并行处理，充分利用 GPU 的并行计算能力。

### 生成过程：Prefill → Decode → 循环

```
Prefill（预填充）：一口气读完你的输入，理解你的意思
    ↓
Decode（解码）：一个字一个字地生成回答
    ↓
把新生成的字拼回去 → 再 Decode → 再拼回去 → ...
    ↓
直到：生成 [EOS]（结束符）或达到 max_tokens 上限
```

这就是为什么 ChatGPT 回答时是一个字一个字出来的——它在做 Decode。

---

## 6. 它怎么被教出来的——训练的故事

一个 LLM 不是"编程"出来的，是"训练"出来的。训练分三步。

### 第一步：预训练（Pre-training）

**相当于：让一个孩子读完整个互联网。**

```
输入：几万亿个 token 的文本（网页、书、代码、论文...）
方法：遮住最后一个 token，让模型猜
例子：给模型看"中国的首都是___"，让它猜"北京"
```

猜对了 → 加强这个路径。猜错了 → 调整参数。

这个阶段**最贵**。GPT-4 级别的预训练成本大约是几亿美元。你需要几万张 GPU 跑几个月。

预训练完成后的模型叫 **Base Model（基础模型）**。它的能力是：**给你一段话的开头，它能合理地续写。** 但它不会聊天——你问它"什么是 Attention"，它可能会续写"是一种神经网络结构，由 Vaswani 等人于 2017 年提出..."也可能续写"这个问题很难，我们先来看看另一个问题..."。它只是在续写，不是在回答。

### 第二步：监督微调（SFT，Supervised Fine-Tuning）

**相当于：请家教，教孩子"对话的格式"。**

```
输入：几万条人工标注的"问题 → 理想回答"
例子：
  问题："什么是 Attention？"
  理想回答："Attention 是 Transformer 的核心机制，它让每个 token..."
```

模型学会：用户问一个问题 → 我应该用问答的方式回应，而不是续写。

这个阶段成本低很多——几万条数据、几千美元。

SFT 完成后的模型叫 **Instruct Model（指令模型）**。它会聊天了，但回答可能不够好——无用的、有害的、不诚实的回答需要被压制。

### 第三步：RLHF / DPO（对齐）

**相当于：教孩子"什么是对的"。**

```
输入：人类对多个回答的偏好排序（回答 A > 回答 B > 回答 C）
方法：让模型倾向生成人类偏好的那种回答
```

这个阶段让模型输出：
- **有用（Helpful）：** 真正解决了用户的问题
- **无害（Harmless）：** 不生成危险、歧视、违法内容
- **诚实（Honest）：** 不知道就说不知道

三个步骤完成后，你得到了一个 **Chat Model**——能够聊天、遵循指令、输出安全内容的最终产品。

### 一张图总结

```
预训练（海量文本）    →   Base Model（会续写）     →  几亿美元
        ↓
SFT（几万条问答）     →   Instruct Model（会聊天）  →  几千美元
        ↓
RLHF（人类偏好）      →   Chat Model（最终产品）    →  几万美元
```

**你已经做过的事：** Phase 4 的 LoRA 微调就是 SFT 的极简版——200 条数据、一张显卡、几分钟。只是规模小了几万倍。

---

## 7. 它有哪些做不到的事

了解 LLM 的局限，比了解它的能力更重要。

| 局限 | 表现 | 为什么 |
|------|------|--------|
| **幻觉** | 自信地编造不存在的事实 | 它是预测引擎，不是数据库 |
| **知识截止** | 不知道训练数据之后发生的事 | 训练数据有截止日期 |
| **数学不行** | 复杂计算容易出错 | 它是语言模型，不是计算器 |
| **上下文限制** | 太长就"忘记"前面说了什么 | 上下文窗口有上限 |
| **不会说"不知道"** | 宁可编一个答案也不会拒绝 | RLHF 没完全解决 |
| **推理链断裂** | 复杂多步推理容易出错 | 每步都有概率出错，积累起来 |
| **成本** | 大规模使用很贵 | GPU 和电力不便宜 |
| **偏见** | 训练数据里的偏见会被模型学到 | 互联网本身就充满了偏见 |

**最重要的教训：永远不要把 LLM 的输出当事实。把它当成一个"非常聪明但偶尔会胡说的同事"。**

---

## 8. 怎么用它——三种方式

### 方式 1：网页聊天（零门槛）

打开 chat.deepseek.com 或 chat.openai.com，打字聊天。

适合：偶尔用用，不需要写代码。

### 方式 2：调 API（有门槛，但灵活）

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-xxx",
    base_url="https://api.deepseek.com",
)

response = client.chat.completions.create(
    model="deepseek-chat",
    messages=[{"role": "user", "content": "用一句话解释 Transformer"}],
    stream=True,
)

for chunk in response:
    print(chunk.choices[0].delta.content or "", end="")
```

适合：把 LLM 集成到你的应用里，批量处理，做自动化。

> 本项目 Phase 1 的 `cli-chat/main.py` 就是这个方式。跑一遍就懂。

### 方式 3：本地部署（最高自由，但也最麻烦）

在你自己的电脑上跑模型。数据不出门，不需要网络，不按 token 付费。

```powershell
ollama pull qwen2.5:7b
ollama run qwen2.5:7b
```

适合：处理敏感数据、高频调用、想完全控制模型行为。

> 本项目 Phase 1-2 覆盖了本地部署和推理引擎。

### 三种方式对比

| | 网页聊天 | API 调用 | 本地部署 |
|---|---|---|---|
| 门槛 | 零 | 需要写代码 | 需要 16GB+ 内存 |
| 能力 | 最强 | 最强 | 取决于你的硬件 |
| 成本 | 免费额度 | 按 token 收费 | 电费 |
| 隐私 | 数据给供应商 | 数据给供应商 | 数据不出电脑 |
| 灵活性 | 低 | 高 | 最高 |

---

## 9. 怎么让它更厉害——RAG、Agent、微调

LLM 有很多局限——不知道你的私有数据、不能执行操作、不能按你想要的方式说话。三种方法解决：

### RAG（检索增强生成）——给它看资料

```
普通 LLM：        你问 → LLM 凭记忆回答（可能不知道、可能编造）

RAG：             你问 → 先搜索你的文档库 → 找到相关内容
                            → 把内容 + 问题一起给 LLM
                            → LLM 基于"刚给它的资料"回答
```

**用 RAG 的场景：** 公司内部知识库问答、产品文档助手、基于特定书籍/法规的回答。

**本项目：** `rag-demo/rag.py`，跑 `--compare` 看差异。

### Agent（智能体）——让它能做事

```
普通 LLM：        你说"帮我算 12345 × 6789" → LLM 可能算错

Agent：           你说"帮我算 12345 × 6789"
                    → LLM 决定：调用计算器工具
                    → 工具计算：= 83,827,205
                    → LLM 看到结果："答案是 83,827,205"
```

**用 Agent 的场景：** 需要调用外部工具（搜索、计算、发邮件、操作数据库）、需要多步骤推理的任务。

**本项目：** `agent-demo/agent.py`。

### 微调（Fine-tuning）——改变它的说话方式

```
原始模型回答："RAII 是 Resource Acquisition Is Initialization 的缩写..."

微调后回答："资源获取即初始化。构造函数拿资源，析构函数释放。
              std::lock_guard<std::mutex> lock(m);"
```

**用微调的场景：** 想要特定的说话风格、特定领域的专业术语、固定的输出格式。

**本项目：** `finetune-demo/train.py`。

### 怎么选

```
你的场景是什么？
  │
  ├─ 模型不知道你的私有数据          → RAG
  ├─ 模型需要调用外部工具            → Agent
  ├─ 模型需要改变说话风格/格式       → 微调
  ├─ 前三种都解决不了               → RAG + Agent + 微调 组合
  └─ 只是想让模型回答得更好一点      → Prompt Engineering（改 prompt 写法）
```

---

## 10. 下一步

现在你已经有了完整的 mental model。接下来：

### 如果你想动手（推荐）
→ 打开 `README.md`，5 分钟跑通第一次 API 调用
→ 然后按 `LLM学习路径_完整版.md` 的 6 个 Phase 逐步深入

### 如果你想深挖某个概念
→ `concepts/tokenization-explained.md` — Token 的方方面面
→ `concepts/embedding-explained.md` — 向量为什么能"理解"语义
→ `concepts/training-story.md` — 训练的三个阶段详解

### 如果你遇到困惑
→ `faq.md` — 新手最常问的 20 个问题

### 如果你想成为专家
→ `advanced-notes/minimal_transformer.py` — 手写一遍 Transformer
→ `advanced-notes/llama-cpp-source-guide.md` — 读 C++ 推理引擎源码
